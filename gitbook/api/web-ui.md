# Web UI API

Endpoints and events exposed by the optional Web UI layer.

## Typical Endpoints

- `GET /plans` – list saved plans
- `POST /run` – execute a plan
- `GET /runs/:id` – run status and logs

## Events

- `task.started`
- `task.completed`
- `task.failed`


